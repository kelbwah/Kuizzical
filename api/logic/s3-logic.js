const queries = ('../queries');

const s3UploadLogic = async (body) => {
    try {

    } catch (err) {
        throw err;
    };
};

const s3DeleteLogic = async (body) => {
    try {

    } catch (err) {
        throw err;
    };
};

module.exports = {
    s3UploadLogic,
    s3DeleteLogic,
};
